input{
file{
path => "{{ frontend_log_file }}"
}
}

output {
   elasticsearch {
       hosts => "https://elastic-dev.pdevops78.online:9200"
       user => "{{ elastic_user }}"
       password => "{{ elastic_password }}"
       index => "frontend-log-%{+yyyy.MM.dd}"
}
}


