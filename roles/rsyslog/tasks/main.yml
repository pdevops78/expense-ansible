- name: copy rysyslog file
  ansible.builtin.template:
    src: rsyslog.conf
    dest: backend.conf

- name: start rsyslog service
  ansible.builtin.systemd_service:
     name: rsyslog
     state: started
     enabled: true
